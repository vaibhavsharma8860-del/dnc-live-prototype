;}if(e.target.id==='person-sort'){state.personSort=e.target.value;render();}if(e.target.id==='blacklist-status'){state.blacklistStatus=e.target.value;state.blacklistPage=1;state.blacklistSelected=new Set();state.blacklistAllMatching=false;render();}if(e.target.id==='blacklist-reason'){state.blacklistReason=e.target.value;state.blacklistPage=1;state.blacklistSelected=new Set();state.blacklistAllMatching=false;render();}if(e.target.id==='blacklist-sort'){state.blacklistSort=e.target.value;render();}if(e.target.id==='status-filter'){state.status=e.target.value;state.page=1;state.selected=new Set();render();}if(e.target.id==='sector-filter'){state.sector=e.target.value;state.page=1;state.selected=new Set();render();}if(e.target.id==='sort'){state.sort=e.target.value;render();}if(e.target.id==='audit-type'){state.auditType=e.target.value;state.auditPage=1;state.auditSelected=new Set();state.auditAllMatching=false;render();}if(e.target.id==='audit-action'){state.auditAction=e.target.value;state.auditPage=1;state.auditSelected=new Set();state.auditAllMatching=false;render();}if(e.target.id==='audit-owner'){state.auditOwner=e.target.value;state.auditPage=1;state.auditSelected=new Set();state.auditAllMatching=false;render();}if(e.target.id==='audit-date'){state.auditDate=e.target.value;state.auditPage=1;render();}if(e.target.id==='audit-sort'){state.auditSort=e.target.value;render();}});
document.addEventListener('keydown',e=>{if(e.key==='Enter'&&document.activeElement?.id==='search-input')runSearch();if(e.key==='Enter'&&document.activeElement?.id==='audit-search')runAuditSearch();if(e.key==='Enter'&&document.activeElement?.id==='idnc-search')runPersonSearch();if(e.key==='Enter'&&document.activeElement?.id==='blacklist-search')runBlacklistSearch();if(e.key==='Tab'){const modal=document.querySelector('.restriction-modal');if(modal){const focusable=[...modal.querySelectorAll('button:not([disabled]),input:not([disabled]),select:not([disabled]),textarea:not([disabled]),[tabindex]:not([tabindex="-1"])')];if(focusable.length){const first=focusable[0],last=focusable[focusable.length-1];if(e.shiftKey&&document.activeElement===first){e.preventDefault();last.focus();}else if(!e.shiftKey&&document.activeElement===last){e.preventDefault();first.focus();}}}}if(e.key==='Escape'&&(state.addType||state.removeStage||state.rlcRemoveStage||state.recordDetail||state.auditPreview||state.queueDetail)){state.addType=null;state.removeStage=null;state.rlcRemoveStage=null;state.recordDetail=null;state.auditPreview=null;state.queueDetail=null;render();}});
window.addEventListener('hashchange',()=>{state.view=({'#cdnc':'cdnc','#idnc':'idnc','#blacklist':'blacklist','#audit':'audit','#bulk-import':'bulk-import','#broadcast-queue':'broadcast-queue','#concurrent':'concurrent','#rlc':'rlc'}[location.hash]||'overview');render();});
render();

runAuditExport=async function(scope){state.auditExportScope=scope;state.auditExportState='running';render();await new Promise(r=>setTimeout(r,650));const selected=state.auditAllMatching?12486:state.auditSelected.size;const shouldFail=!state.auditExportFailedOnce&&(scope==='view'||(scope==='selected'&&selected>1000));if(shouldFail){state.auditExportFailedOnce=true;state.auditExportState='failed';state.announcement='Audit export failed. Query, filters and selection are preserved.';}else{state.auditExportState='success';state.announcement='Audit export started.';}render();};